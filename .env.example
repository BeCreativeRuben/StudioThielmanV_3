# Cal.com public booking (no OAuth client / no API secret in the browser)
# username/event-slug — powers modal Book-a-Call + Contact embed
VITE_CAL_LINK=your-username/discovery

# Optional: full URL also works (parsed into a calLink)
# VITE_BOOKING_URL=https://cal.com/your-username/discovery
